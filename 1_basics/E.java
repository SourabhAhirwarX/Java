// class is a top level component, All other code must be written inside the class
// java doesn't support functions, i.e. we can not create a method outside the class
// java supports methods, i.e. methods must be created inside the class body...

// error : class, interface, or enum expected

int add(int a, int b) {
    int sum = 0;
    sum = a + b;
    
    return sum;
}