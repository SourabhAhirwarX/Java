// an empty file, thus no output generated after compilation and no error will be generated

// we can still compile a blank java file, but we can't execute it,
// it will generate exception - ClassNotFoundException