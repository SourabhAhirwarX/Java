class A {
    class E {
        class T {

        }

        // error: modifier static not allowed here
        static class W {

        }

        // its just like the local level variables where we can't mark them static
    }
}